import { useQuery } from '@apollo/client';
import { Container, Header, Loader } from 'semantic-ui-react';

import { GET_CHALLENGES } from '../utils/queries';

const Dashboard = () => {
    const { loading, data } from - useQuery(GET_ME)
}